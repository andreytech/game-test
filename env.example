# Minimal environment configuration for development

# Application Environment
APP_ENV=dev
APP_SECRET=dev-secret-key-change-in-production

# Database (required by Symfony but not used)
DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"

# Cache and Session (using array for simplicity)
CACHE_DSN=array
SESSION_DSN=array

# Logging
LOG_LEVEL=info
